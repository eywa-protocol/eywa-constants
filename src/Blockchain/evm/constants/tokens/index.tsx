import wrappedNativeTokens from './wrappedNativeTokens.json'
import config from '../config.json'
// const defaultTokens = Object.values(config).map(v => )

const tokens = {
    wrappedNativeTokens
}

export {tokens}